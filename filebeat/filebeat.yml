filebeat.inputs:
  - type: log
    paths:
      - /var/log/app101/app.log
    multiline:
      pattern: '^{'
      negate: true
      match: after

output.logstash:
  hosts: ["logstash:5044"]

setup.kibana:
  host: "kibana:5601"

# processors:
#   - add_docker_metadata: ~
#   - add_host_metadata: ~